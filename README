-Execute Test.py to run the driver suite.-

CachAddress.py handles a class to represent a stored address in the cache

SetAssCache.py handles the base class for caches

WriteThruCache.py handles the child class for the set associative cache class which simulates a write-through cache

WriteBackCache.py handles the child class for the set associative cache class which simulates a write-back cache